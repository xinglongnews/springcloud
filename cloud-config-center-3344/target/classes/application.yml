server:
  port: 3344

spring:
  application:
    name: cloud-config-center
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/newly_born/springcloud-config.git
          search-paths:
            - springcloud-config
          force-pull: true
      #          username: xinglongnews
#          password: rasSbE%f4zNFlmTS
      label: master
  rabbitmq:
    host: 121.43.169.252
    port: 5673
    username: guest
    password: guest


eureka:
  client:
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "bus-refresh"
